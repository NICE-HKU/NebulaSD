# GPU acceptance

`autonomous_engine_gpu.py` drives real Engine requests and compares against `canonical_reference_gpu.py`, with optional EOS, delayed publication and multiple cohorts. Model and calibrated cost paths are explicit. See the parent test README for invocation.
