# Native scheduler

Numeric cost prediction and scheduling kernels. The Python build tool generates `scheduler_schema.h`; rebuild when schema changes. C++ consumes numeric buffers, not shared tables directly.
