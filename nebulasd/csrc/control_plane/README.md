# Native shared memory

Atomic publication, stable reads and ring primitives for the Python control plane. `native.cpp` is the build entry. Preserve ABI/layout compatibility with Python table definitions.
