# Workers

WORK authorization, dependencies, Runtime, Banks, DMA and publication. `isolated.py` connects control-side input compilers to execution-only GPU backends. `runtime.py` owns local progression; Engine does not micromanage each compute/copy phase.
