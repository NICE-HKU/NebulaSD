# Target worker

`inputs.py` compiles prefill/verify inputs; `backend.py` executes model work; `control.py`, `service.py` and `process.py` provide process/transport wiring; `publication.py` commits Target decisions and cumulative output.
