# Draft worker

`inputs.py` restores and reconciles Draft request state into CPU plans; `backend.py` executes proposals; `publication.py` publishes results and snapshots. Draft HostKV belongs to the Draft model only.
