# Engine

Admission, owner request state, accepted facts, scheduling/dispatch, output and retirement. Start at `bootstrap.py`, `core.py` and `work_progress.py`. This layer does not run model forwards.
