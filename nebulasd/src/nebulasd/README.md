# Package

`api.py` exposes Engine construction; `config.py` defines validated configuration. Engine, scheduler, table, IPC, KV and Worker modules have separate ownership boundaries. See the root README for setup.
