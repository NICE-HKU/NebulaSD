# Scheduler

Candidate eligibility, readiness/cost prediction, batching and placement. `completion.py` and `service_interval.py` select policy; `native_completion.py` projects inputs to C++. Decisions do not replace actual execution dependency checks.
