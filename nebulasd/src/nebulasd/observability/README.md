# Observability

Metrics, bounded traces and diagnostic/profile summaries. Observations must not become correctness dependencies. Host timing, CUDA event envelopes and kernel-active time are different; missing GPU data is not zero GPU work. Profiling changes measurement overhead.
