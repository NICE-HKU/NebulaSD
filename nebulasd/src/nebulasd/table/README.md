# Shared tables

Layout/ABI declarations, writer ownership, stable row publication/reading and changed-row hints. Validate epochs and dependencies when consuming facts. Hint loss/overflow must not lose authoritative state.
